import React from 'react';

// ConnectionStatus component removed to eliminate visual noise
// WebSocket functionality still works in the background
const ConnectionStatus = () => {
  // Return null to not render anything
  return null;
};

export default ConnectionStatus;
